import  {InputContainer, InputText, IconContainer} from './styles.js'

const Input = ({leftIcon, name, ...rest}) => {
  return <>
  <InputContainer>
    <InputText/>
  </InputContainer>
  </>;
};

export { Input };
